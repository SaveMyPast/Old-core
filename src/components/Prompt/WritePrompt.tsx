import * as React from "react";
import { PromptData } from "../../services/interfaces";

const WritePrompt = ({ promptData }: { promptData: PromptData }) => {
  return <></>;
};

export default WritePrompt;
